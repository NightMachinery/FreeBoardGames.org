# Self-hosting HTTP workflow

This legacy HTTP-specific guide has been replaced by the canonical self-hosting workflow in [`self-hosting.md`](./self-hosting.md).

For the default HTTP deployment, run:

```zsh
./self_host.zsh setup http://fbg.pinky.lilf.ir
```
